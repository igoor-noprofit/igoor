document.addEventListener('DOMContentLoaded', function() {
    const app = new Vue({
        el: '#app',
        components: {
            // The components list will be replaced dynamically in the HTML by Python
        }
    });
});