# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[('dictionary','dictionary'), ('css', 'css'), ('img', 'img'), ('js', 'js'), ('plugins', 'plugins'), ('tools', 'tools'), ('.env', '.'), ('README.md', '.'), ('requirements.txt', '.'), ('index.html', '.')],
    hiddenimports=['pydub.playback','speechify','pymupdf4llm', 'PIL.ImageGrab','webrtcvad','pyaudio', 'vosk', 'prompt_manager', 'context_manager', 'settings_manager', 'llm_manager','db_manager','pydantic.deprecated.decorator'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=['torch.utils.tensorboard'],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='igoor',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon="img/logo_ig_lxL_icon.ico"
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='igoor',
)
