FEATURES
- split screen (chinois / libanais) => see modified prompt
- IDK function: ask the LLM to answer "I don't know" if the first phrase does not make any particular sense?
- NOISE REDUCTION: ask the LLM to answer "delete last message" if the last message does not make any particular sense (ex. "mmm" or weird phrase)

- FALLBACK LLM ?